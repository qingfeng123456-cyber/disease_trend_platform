[pytest]
markers =
    spark: tests that require PySpark
    integration: tests that require external services such as HDFS or Spark runtime
    remote: tests that require a real SSH-accessible Ubuntu host
