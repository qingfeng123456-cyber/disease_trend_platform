requests>=2.31,<3
pandas>=2.0,<3
numpy>=1.24,<3
PyYAML>=6.0,<7
beautifulsoup4>=4.12,<5
lxml>=5.0,<6
Flask>=3.0,<4
Flask-Cors>=4.0,<6
pytest>=8.0,<9
statsmodels>=0.14,<1
scikit-learn>=1.3,<2
joblib>=1.3,<2
pyarrow>=14,<20
# Install PySpark separately with the same version as the server Spark if needed.
# Example: pyspark==<spark-submit version>
# torch is optional for the LSTM demo and is not required for the basic project.
