PyYAML>=6.0,<7
requests>=2.31,<3
pandas>=2.0,<3
pyarrow>=14,<20
pytest>=8.0,<9
# PySpark is normally provided by the Ubuntu Spark installation used by spark-submit.
# Install pyspark here only if your Spark distribution requires it in the Conda env.
